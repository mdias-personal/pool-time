spring:
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  datasource:
    url: "jdbc:postgresql://localhost:5432/"
    username: "myusername"
    password: "mypassword"
server:
  port: 82